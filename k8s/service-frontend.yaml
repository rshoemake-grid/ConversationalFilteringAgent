apiVersion: v1
kind: Service
metadata:
  name: conversational-filtering-agent-frontend
  labels:
    app: conversational-filtering-agent-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: conversational-filtering-agent-frontend

apiVersion: v1
kind: Service
metadata:
  name: conversational-filtering-agent-backend
  labels:
    app: conversational-filtering-agent-backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: conversational-filtering-agent-backend
